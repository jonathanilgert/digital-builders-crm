# API key required for /api/integrations/* endpoints (e.g. OpenClaw / cron jobs).
# Generate a long random string: `openssl rand -hex 32`
OPENCLAW_API_KEY=

# Optional - server port (defaults to 3001)
PORT=3001
